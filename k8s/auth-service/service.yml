# [K8s] Auth Service: 클러스터 내부 전용 Service
# Gateway 만 auth-service 에 접근하므로 ClusterIP 로 충분하다.
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: msa-training
spec:
  selector:
    app: auth-service
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
